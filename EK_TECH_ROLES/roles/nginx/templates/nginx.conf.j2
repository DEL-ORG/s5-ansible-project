# /etc/nginx/nginx.conf

events {
    worker_connections 768;
}

http {
    server {
        listen 8585;
        server_name localhost;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
