---
- name: configure bastion servers
  hosts: #TBD
  become: yes
  become_user: root  
  roles:
     -user
     -patching
     -packages
     -docker
     -docker-compose
     -terraform
     -kubens_kubectl
     -helm
     -awscli
     -bastion
     -reboot