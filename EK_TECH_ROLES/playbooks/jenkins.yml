---
- name: install and configure Jenkins
  hosts: #TBD
  become: yes
  become_user: root  
  roles:
     -user
     -patching
     -packages
     -docker
     -docker-compose
     -jenkins
     -reboot